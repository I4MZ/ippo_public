# 자동으로 줄바꿈 문자를 처리합니다 (LF로 정규화).
* text=auto

# 특정 파일 확장자를 명시적으로 텍스트로 처리하고, LF를 사용합니다.
*.txt text eol=lf
*.md text eol=lf
*.html text eol=lf
*.css text eol=lf
*.js text eol=lf
*.py text eol=lf
*.sh text eol=lf

# Windows 스크립트 파일에 대해 CRLF를 사용합니다.
*.bat text eol=crlf
*.cmd text eol=crlf

# 이진 파일로 명시적으로 처리할 확장자를 지정합니다. (줄바꿈 처리 없음)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.mov binary
*.mp4 binary
*.doc binary
*.pdf binary
*.zip binary
